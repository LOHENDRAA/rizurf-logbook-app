# 05-ui-mockups — student workspace UI mockups

## Purpose

Early UI mockups of the internship portal's student workspace: the
dashboard and the daily entry screen. (Originally in `Flow/Daily Log/` —
despite the folder name, these are interface screenshots, not flow
diagrams.)

## What belongs

UI screenshots/mockups only.

## What does NOT belong

Flow diagrams → see `../01-baseline-workflows/`. Feature Input→Process→
Output diagrams → see `../03-dynamic-logbook/` and
`../04-manual-compilation/`.

## Files

| New name | Original path | Depicted intent | Related v3 concept |
| --- | --- | --- | --- |
| `student-workspace-dashboard-overview.png` | `Flow/Daily Log/Pasted image.png` | Student workspace dashboard: placement cards (university, company, dates) and daily-log entry point. | Closest to v3 `/dashboard` (placement + "open today's log"). |
| `daily-log-entry-screen-draft.png` | `Flow/Daily Log/Pasted image (2).png` | Daily entry screen for 2026-09-14 (DRAFT): free-text area, word count, mark-complete action, day navigation. | Closest to v3 `/journal/days/:date` (draft → complete). |
