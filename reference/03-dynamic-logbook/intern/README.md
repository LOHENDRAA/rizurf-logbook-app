# 03-dynamic-logbook / intern — dynamic-track intern features

## Purpose

Intern-side zooms of the dynamic track: one-time setup, then daily entries
that link automatically into the weekly record.

## What belongs

Dynamic-track intern diagrams.

## What does NOT belong

Manual-track intern diagrams (daily log + manual weekly compilation) →
see `../../04-manual-compilation/intern/`.

## Files

| New name | Original path | Depicted intent | Related v3 concept |
| --- | --- | --- | --- |
| `intern-setup-dynamic-registration.png` | `Component/InternSetup.png` | Intern one-time setup: profile → placement → schedule (Input→Process→Output). | Onboarding before `/dashboard`. |
| `intern-dynamic-log-entry.png` | `Component/InternDynamicLogEntry.png` | Dynamic log entry: auto-linked daily input with validation and confirmation. | v3 daily entry (`/journal/days/:date`). |
